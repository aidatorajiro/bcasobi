/* 
 * Usage (A simple paxos implementation):
 * ```
 * let v = peer('validator')
 * let c = peer('client')
 * v.state('', 'number')
 * v.state('', 'number')
 * ```
 */

export class Peer () {
  
}

/*
 * @member roles List of roles of the network.
 * @member 
 */
export class Network () {
  
}